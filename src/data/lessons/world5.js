// World 5: Python Path — Python fundamentals (simulated in JS)
export const world5Lessons = [
  {
    id: 'w5_l1', worldId: 'world5', order: 1,
    title: 'Why Python',
    description: 'Understand what Python excels at and how it differs from JavaScript.',
    type: 'review', xpReward: 60, language: 'javascript',
    instruction: `**Python** is one of the most popular programming languages in the world. It's known for:\n\n- **Readability** — Python uses indentation instead of braces, making code clean and scannable\n- **Versatility** — Web backends, data science, AI/ML, automation, scripting\n- **Massive ecosystem** — pip (Python's package manager) has 400,000+ packages\n- **Beginner-friendly** — Simple syntax, great for learning\n\nKey differences from JavaScript:\n- Python uses \`def\` instead of \`function\`\n- Indentation is required (not optional like JS braces)\n- Python has tuples, sets, and list comprehensions\n- No semicolons needed\n\nAnswer these questions about Python.`,
    starterCode: '', hints: ['Python is known for readability and versatility.', 'Think about what makes Python different from JavaScript.'],
    concepts: ['python', 'python-basics'], solution: '',
    questions: [
      { question: 'What is Python best known for?', options: ['Being the fastest language', 'Readability and clean syntax', 'Only working in web browsers', 'Having the smallest standard library'], answer: 1, explanation: 'Python is famous for its clean, readable syntax. The use of indentation for code blocks makes it one of the easiest languages to read and write.' },
      { question: 'Which is NOT a common use case for Python?', options: ['Data science and machine learning', 'Web backend development', 'iOS app development', 'Automation and scripting'], answer: 2, explanation: 'While Python can technically be used for mobile development, iOS apps are primarily built with Swift/Objective-C. Python shines in data science, web backends (Django, Flask), and automation.' },
      { question: 'How does Python handle code blocks differently from JavaScript?', options: ['Uses square brackets', 'Uses indentation (whitespace) instead of curly braces', 'Uses parentheses', 'Uses XML tags'], answer: 1, explanation: 'Python uses indentation (typically 4 spaces) to define code blocks, while JavaScript uses curly braces {}. This enforces readable, consistent formatting.' },
    ],
  },
  {
    id: 'w5_l2', worldId: 'world5', order: 2,
    title: 'Python Basics',
    description: 'Variables, types, and printing — Python-style in JavaScript.',
    type: 'concept', xpReward: 75, language: 'javascript',
    instruction: `In Python, you'd write:\n\`\`\`python\nname = "Alice"\nage = 25\nis_student = True\nprint(f"Name: {name}, Age: {age}")\n\`\`\`\n\nIn JavaScript, the equivalent is:\n\`\`\`javascript\nconst name = "Alice";\nconst age = 25;\nconst isStudent = true;\nconsole.log(\`Name: \${name}, Age: \${age}\`);\n\`\`\`\n\n**Your task:** Create three variables (Python-style naming with underscores):\n- \`user_name\` = "CodeForger"\n- \`skill_level\` = 5\n- \`is_active\` = true\n\nThen log: \`User: CodeForger, Level: 5, Active: true\``,
    starterCode: '// Create variables with Python-style snake_case naming\n\n// Log the formatted string\n',
    hints: ['Use const for all three variables', 'Use template literals: console.log(`User: ${user_name}, Level: ${skill_level}, Active: ${is_active}`)'],
    concepts: ['python', 'python-variables', 'python-naming'],
    solution: 'const user_name = "CodeForger";\nconst skill_level = 5;\nconst is_active = true;\nconsole.log(`User: ${user_name}, Level: ${skill_level}, Active: ${is_active}`);',
    validation: { type: 'output', expected: ['User: CodeForger, Level: 5, Active: true'] },
  },
  {
    id: 'w5_l3', worldId: 'world5', order: 3,
    title: 'Control Flow in Python',
    description: 'if/elif/else — Python-style conditional logic.',
    type: 'concept', xpReward: 75, language: 'javascript',
    instruction: `Python uses \`if/elif/else\` (not \`else if\`):\n\`\`\`python\nif score >= 90:\n    grade = "A"\nelif score >= 80:\n    grade = "B"\nelse:\n    grade = "C"\n\`\`\`\n\n**Your task:** Write a function \`getGrade(score)\` that returns:\n- "A" for 90+\n- "B" for 80-89\n- "C" for 70-79\n- "F" for below 70\n\nTest it by logging grades for scores: 95, 85, 72, 60`,
    starterCode: '// Write getGrade function\n\n// Test it:\n// console.log(getGrade(95));\n// console.log(getGrade(85));\n// console.log(getGrade(72));\n// console.log(getGrade(60));\n',
    hints: ['Use if/else if/else chain', 'Check >= 90 first, then >= 80, then >= 70, else F'],
    concepts: ['python', 'python-conditionals', 'js-conditionals'],
    solution: 'function getGrade(score) {\n  if (score >= 90) return "A";\n  else if (score >= 80) return "B";\n  else if (score >= 70) return "C";\n  else return "F";\n}\nconsole.log(getGrade(95));\nconsole.log(getGrade(85));\nconsole.log(getGrade(72));\nconsole.log(getGrade(60));',
    validation: { type: 'output', expected: ['A', 'B', 'C', 'F'] },
  },
  {
    id: 'w5_l4', worldId: 'world5', order: 4,
    title: 'Functions in Python',
    description: 'Write functions with default parameters and docstrings.',
    type: 'concept', xpReward: 100, language: 'javascript',
    instruction: `Python functions use \`def\` and support default parameters:\n\`\`\`python\ndef greet(name, greeting="Hello"):\n    """Greet a person with a custom greeting."""\n    return f"{greeting}, {name}!"\n\`\`\`\n\n**Your task:** Write a function \`formatPrice(amount, currency = "$", decimals = 2)\` that:\n- Formats a number as a price string\n- Uses default currency "$" and 2 decimals\n\nTest:\n- formatPrice(29.9) → "$29.90"\n- formatPrice(100, "€") → "€100.00"\n- formatPrice(5.5, "£", 0) → "£6"`,
    starterCode: '// Write formatPrice with default parameters\n\n// Test:\n// console.log(formatPrice(29.9));\n// console.log(formatPrice(100, "€"));\n// console.log(formatPrice(5.5, "£", 0));\n',
    hints: ['function formatPrice(amount, currency = "$", decimals = 2)', 'Use amount.toFixed(decimals) to format', 'Return currency + amount.toFixed(decimals)'],
    concepts: ['python', 'python-functions', 'default-parameters'],
    solution: 'function formatPrice(amount, currency = "$", decimals = 2) {\n  return currency + amount.toFixed(decimals);\n}\nconsole.log(formatPrice(29.9));\nconsole.log(formatPrice(100, "€"));\nconsole.log(formatPrice(5.5, "£", 0));',
    validation: { type: 'output', expected: ['$29.90', '€100.00', '£6'] },
  },
  {
    id: 'w5_l5', worldId: 'world5', order: 5,
    title: 'Data Structures',
    description: 'Lists, dictionaries, and sets — Python concepts in JavaScript.',
    type: 'concept', xpReward: 100, language: 'javascript',
    instruction: `Python data structures map to JS like this:\n- **List** → Array: \`[1, 2, 3]\`\n- **Dict** → Object: \`{"key": "value"}\`\n- **Set** → Set: \`{1, 2, 3}\`\n- **Tuple** → no direct equivalent (use frozen arrays)\n\n**Your task:** Create:\n1. A \`fruits\` array with ["apple", "banana", "cherry"]\n2. A \`scores\` object with {math: 90, science: 85, english: 92}\n3. A \`uniqueNums\` Set from [1, 2, 2, 3, 3, 3]\n\nLog:\n- Fruits count: 3\n- Math score: 90\n- Unique numbers: 3`,
    starterCode: '// Create the three data structures\n\n// Log the results\n',
    hints: ['const fruits = ["apple", "banana", "cherry"]', 'const scores = { math: 90, science: 85, english: 92 }', 'const uniqueNums = new Set([1, 2, 2, 3, 3, 3])'],
    concepts: ['python', 'python-data-structures', 'js-arrays', 'js-objects'],
    solution: 'const fruits = ["apple", "banana", "cherry"];\nconst scores = { math: 90, science: 85, english: 92 };\nconst uniqueNums = new Set([1, 2, 2, 3, 3, 3]);\nconsole.log("Fruits count: " + fruits.length);\nconsole.log("Math score: " + scores.math);\nconsole.log("Unique numbers: " + uniqueNums.size);',
    validation: { type: 'output', expected: ['Fruits count: 3', 'Math score: 90', 'Unique numbers: 3'] },
  },
  {
    id: 'w5_l6', worldId: 'world5', order: 6,
    title: 'File I/O Concepts',
    description: 'Understand reading and writing files in Python.',
    type: 'review', xpReward: 60, language: 'javascript',
    instruction: `Python makes file operations simple:\n\`\`\`python\n# Reading\nwith open("data.txt", "r") as f:\n    content = f.read()\n\n# Writing\nwith open("output.txt", "w") as f:\n    f.write("Hello!")\n\n# JSON\nimport json\nwith open("data.json") as f:\n    data = json.load(f)\n\`\`\`\n\nThe \`with\` statement automatically closes the file when done. Python can also read/write CSV files with the \`csv\` module.`,
    starterCode: '', hints: ['The with statement handles file closing automatically.'],
    concepts: ['python', 'python-file-io'], solution: '',
    questions: [
      { question: 'What does the Python "with" statement do for file operations?', options: ['Makes files read-only', 'Automatically closes the file when the block ends', 'Encrypts the file', 'Creates a backup'], answer: 1, explanation: 'The "with" statement ensures the file is properly closed when the code block exits, even if an error occurs. It\'s Python\'s preferred way to handle files.' },
      { question: 'Which Python module handles JSON files?', options: ['csv', 'json', 'file', 'data'], answer: 1, explanation: 'The json module provides json.load() to read and json.dump() to write JSON files. It\'s part of Python\'s standard library.' },
      { question: 'What mode would you use to append to an existing file?', options: ['"r" (read)', '"w" (write)', '"a" (append)', '"x" (exclusive)'], answer: 2, explanation: '"a" mode appends to the end of a file. "w" would overwrite the entire file, and "r" is for reading only.' },
    ],
  },
  {
    id: 'w5_l7', worldId: 'world5', order: 7,
    title: 'Building CLI Tools',
    description: 'Build a command-line argument parser.',
    type: 'concept', xpReward: 100, language: 'javascript',
    instruction: `Python CLI tools use \`argparse\` or \`sys.argv\` to handle command-line arguments. Let's simulate a command parser.\n\n**Your task:** Write a function \`parseCommand(input)\` that:\n- Takes a string like "add Buy milk" or "list" or "done 2"\n- Returns an object: { action, args }\n- "add Buy milk" → { action: "add", args: "Buy milk" }\n- "list" → { action: "list", args: "" }\n- "done 2" → { action: "done", args: "2" }\n\nTest all three commands.`,
    starterCode: '// Build a command parser\n\n// Test:\n// console.log(JSON.stringify(parseCommand("add Buy milk")));\n// console.log(JSON.stringify(parseCommand("list")));\n// console.log(JSON.stringify(parseCommand("done 2")));\n',
    hints: ['Split the input string: const parts = input.split(" ")', 'action is parts[0], args is parts.slice(1).join(" ")', 'Handle the case where there are no args'],
    concepts: ['python', 'python-cli', 'string-parsing'],
    solution: 'function parseCommand(input) {\n  const parts = input.split(" ");\n  return { action: parts[0], args: parts.slice(1).join(" ") };\n}\nconsole.log(JSON.stringify(parseCommand("add Buy milk")));\nconsole.log(JSON.stringify(parseCommand("list")));\nconsole.log(JSON.stringify(parseCommand("done 2")));',
    validation: { type: 'output', expected: ['{"action":"add","args":"Buy milk"}', '{"action":"list","args":""}', '{"action":"done","args":"2"}'] },
  },
  {
    id: 'w5_l8', worldId: 'world5', order: 8,
    title: 'Python with Claude Code',
    description: 'Write a prompt for a Python data processing script.',
    type: 'prompt', xpReward: 100, language: 'javascript',
    instruction: `Claude Code can generate Python scripts just as easily as JavaScript.\n\n**Your task:** Write a prompt asking Claude Code to create a Python script that:\n- Reads a CSV file of student grades (name, math, science, english)\n- Calculates each student's average\n- Finds the top student\n- Writes results to a new CSV file\n\nBe specific about the input format, calculations, and output.`,
    starterCode: '', hints: ['Mention CSV format: name,math,science,english', 'Specify the calculation: average of 3 subjects', 'Include the output format: name,average in a new file'],
    concepts: ['python', 'prompt-engineering', 'python-data-processing'], solution: '',
    rubric: {
      requiredConcepts: [
        { terms: ['csv', 'file', 'read'], label: 'CSV file reading', weight: 2 },
        { terms: ['average', 'mean', 'calculate'], label: 'Average calculation', weight: 2 },
        { terms: ['student', 'name', 'grade'], label: 'Student data', weight: 1 },
        { terms: ['write', 'output', 'save', 'result'], label: 'Output file', weight: 2 },
        { terms: ['python', 'script'], label: 'Python language', weight: 1 },
      ],
      specificityMarkers: [
        { terms: ['math', 'science', 'english', 'column'], weight: 1 },
        { terms: ['top', 'highest', 'best', 'maximum'], weight: 1 },
        { terms: ['header', 'row', 'format'], weight: 1 },
      ],
      minLength: 50,
    },
    simulatedResponses: {
      low: 'import csv\nwith open("grades.csv") as f:\n    reader = csv.reader(f)\n    for row in reader:\n        print(row)',
      medium: 'import csv\n\nwith open("grades.csv") as f:\n    reader = csv.DictReader(f)\n    students = []\n    for row in reader:\n        avg = (int(row["math"]) + int(row["science"]) + int(row["english"])) / 3\n        students.append({"name": row["name"], "average": round(avg, 1)})\n\ntop = max(students, key=lambda s: s["average"])\nprint(f"Top student: {top[\'name\']} ({top[\'average\']})")\n\nwith open("results.csv", "w", newline="") as f:\n    writer = csv.DictWriter(f, fieldnames=["name", "average"])\n    writer.writeheader()\n    writer.writerows(students)',
      high: 'import csv\nfrom pathlib import Path\n\ndef process_grades(input_file: str, output_file: str) -> dict:\n    """Process student grades from CSV and write results."""\n    students = []\n    \n    with open(input_file, newline="") as f:\n        reader = csv.DictReader(f)\n        for row in reader:\n            scores = [int(row["math"]), int(row["science"]), int(row["english"])]\n            avg = round(sum(scores) / len(scores), 1)\n            students.append({"name": row["name"], "average": avg})\n    \n    students.sort(key=lambda s: s["average"], reverse=True)\n    top = students[0]\n    \n    with open(output_file, "w", newline="") as f:\n        writer = csv.DictWriter(f, fieldnames=["name", "average"])\n        writer.writeheader()\n        writer.writerows(students)\n    \n    return {"top_student": top["name"], "top_average": top["average"], "total": len(students)}\n\nif __name__ == "__main__":\n    result = process_grades("grades.csv", "results.csv")\n    print(f"Processed {result[\'total\']} students")\n    print(f"Top student: {result[\'top_student\']} ({result[\'top_average\']})")',
    },
    validation: { type: 'output', expected: [] },
  },
  {
    id: 'w5_l9', worldId: 'world5', order: 9,
    title: 'Building a Text Analyzer',
    description: 'Build a complete text statistics tool.',
    type: 'concept', xpReward: 150, language: 'javascript',
    instruction: `Build a \`analyzeText(text)\` function that returns statistics about a string:\n\n- Word count\n- Character count (excluding spaces)\n- Sentence count (count periods, exclamation marks, question marks)\n- Average word length\n\nTest with: "Hello world. This is a test! How are you?"\n\nExpected output:\nWords: 9\nChars: 32\nSentences: 3\nAvg word length: 3.6`,
    starterCode: '// Build the text analyzer\nfunction analyzeText(text) {\n  // Your code here\n}\n\nconst sample = "Hello world. This is a test! How are you?";\n// const stats = analyzeText(sample);\n// console.log("Words: " + stats.words);\n// console.log("Chars: " + stats.chars);\n// console.log("Sentences: " + stats.sentences);\n// console.log("Avg word length: " + stats.avgWordLength);\n',
    hints: ['Split by spaces for words: text.split(/\\s+/).filter(Boolean)', 'Remove spaces for char count: text.replace(/\\s/g, "").length', 'Count sentence enders: text.match(/[.!?]/g).length'],
    concepts: ['python', 'string-processing', 'js-functions'],
    solution: 'function analyzeText(text) {\n  const words = text.split(/\\s+/).filter(Boolean);\n  const chars = text.replace(/\\s/g, "").length;\n  const sentences = (text.match(/[.!?]/g) || []).length;\n  const avgWordLength = (words.reduce((sum, w) => sum + w.replace(/[^a-zA-Z]/g, "").length, 0) / words.length).toFixed(1);\n  return { words: words.length, chars, sentences, avgWordLength: parseFloat(avgWordLength) };\n}\nconst sample = "Hello world. This is a test! How are you?";\nconst stats = analyzeText(sample);\nconsole.log("Words: " + stats.words);\nconsole.log("Chars: " + stats.chars);\nconsole.log("Sentences: " + stats.sentences);\nconsole.log("Avg word length: " + stats.avgWordLength);',
    validation: { type: 'output', expected: ['Words: 9', 'Chars: 32', 'Sentences: 3', 'Avg word length: 3.6'] },
  },
  // BOSS
  {
    id: 'w5_boss', worldId: 'world5', order: 10,
    title: 'Python Forge',
    description: 'Prove your Python and data processing skills.',
    type: 'boss', xpReward: 500, language: 'javascript',
    instruction: 'The Python Forge tests your ability to write functions, process data, and understand Python concepts.',
    starterCode: '', hints: [], concepts: ['python', 'python-functions', 'python-data-structures'],
    solution: '', validation: { type: 'output', expected: [] },
    phases: [
      {
        type: 'concept', title: 'Phase 1: Default Parameters', xpReward: 150, language: 'javascript',
        instruction: 'Write a function `createUser(name, role = "member", active = true)` that returns a formatted string.\n\nTest:\n- createUser("Alice") → "Alice (member) - active"\n- createUser("Bob", "admin") → "Bob (admin) - active"\n- createUser("Eve", "guest", false) → "Eve (guest) - inactive"',
        starterCode: '// Write createUser with defaults\n',
        hints: ['Use default parameters in the function signature', 'Return `${name} (${role}) - ${active ? "active" : "inactive"}`'],
        concepts: ['python-functions', 'default-parameters'],
        solution: 'function createUser(name, role = "member", active = true) {\n  return `${name} (${role}) - ${active ? "active" : "inactive"}`;\n}\nconsole.log(createUser("Alice"));\nconsole.log(createUser("Bob", "admin"));\nconsole.log(createUser("Eve", "guest", false));',
        validation: { type: 'output', expected: ['Alice (member) - active', 'Bob (admin) - active', 'Eve (guest) - inactive'] },
      },
      {
        type: 'debug', title: 'Phase 2: Fix the Data Processor', xpReward: 150, language: 'javascript',
        instruction: 'This function should calculate the sum of even numbers in an array. Fix the bugs!\n\nExpected: sumEven([1,2,3,4,5,6]) → "Sum of evens: 12"',
        starterCode: 'function sumEven(nums) {\n  let sum = 0;\n  for (let i = 0; i <= nums.length; i++) { // BUG\n    if (nums[i] % 2 === 1) { // BUG: wrong condition\n      sum += nums[i];\n    }\n  }\n  return sum;\n}\nconsole.log("Sum of evens: " + sumEven([1,2,3,4,5,6]));',
        hints: ['Loop should be i < nums.length (not <=)', 'Even numbers have remainder 0 when divided by 2, not 1'],
        concepts: ['python', 'js-debugging', 'js-loops'],
        solution: 'function sumEven(nums) {\n  let sum = 0;\n  for (let i = 0; i < nums.length; i++) {\n    if (nums[i] % 2 === 0) {\n      sum += nums[i];\n    }\n  }\n  return sum;\n}\nconsole.log("Sum of evens: " + sumEven([1,2,3,4,5,6]));',
        validation: { type: 'output', expected: ['Sum of evens: 12'] },
      },
      {
        type: 'review', title: 'Phase 3: Python Best Practices', xpReward: 200, language: 'javascript',
        instruction: 'Answer questions about Python development best practices.',
        starterCode: '', hints: ['Think about readability and maintainability.'],
        concepts: ['python'],
        solution: '',
        questions: [
          { question: 'In Python, what is the standard naming convention for variables and functions?', options: ['camelCase', 'snake_case', 'PascalCase', 'UPPER_CASE'], answer: 1, explanation: 'Python uses snake_case for variables and functions (e.g., my_variable, calculate_total). PascalCase is used for class names, and UPPER_CASE for constants.' },
          { question: 'Which Python tool manages packages and dependencies?', options: ['npm', 'pip', 'brew', 'cargo'], answer: 1, explanation: 'pip is Python\'s package installer. You use "pip install package_name" to install third-party libraries, similar to how npm works for JavaScript.' },
        ],
      },
    ],
  },
];
